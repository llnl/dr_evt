name: DR_EVT Test Suite

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:  # Allow manual trigger

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build and Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        compiler: [gcc, clang]
        include:
          - compiler: gcc
            cc: gcc-11
            cxx: g++-11
          - compiler: clang
            cc: clang-14
            cxx: clang++-14

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            libboost-all-dev \
            python3 \
            python3-pip \
            libopenmpi-dev \
            openmpi-bin \
            libprotobuf-dev \
            protobuf-compiler \
            libgrpc++-dev \
            protobuf-compiler-grpc \
            ${{ matrix.cc }} \
            ${{ matrix.cxx }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Display versions
        run: |
          echo "=== Compiler versions ==="
          ${{ matrix.cc }} --version
          ${{ matrix.cxx }} --version
          cmake --version
          python3 --version
          echo "=== MPI version ==="
          mpirun --version
          echo "=== Boost version ==="
          dpkg -l | grep libboost-dev

      - name: Create Python virtual environment
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip

      - name: Configure CMake
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: |
          source venv/bin/activate
          # Explicitly pin Python3_EXECUTABLE to the active venv's
          # interpreter. find_package(Python3) in SetupPybind11.cmake
          # does not reliably pick up an activated venv on its own across
          # CMake versions/environments - without this, it can silently
          # resolve to a different system Python (e.g. Ubuntu 24.04's
          # default 3.12) even with the venv active, producing a
          # dr_evt.cpython-312-*.so that the venv's own 3.9 interpreter
          # can never import (compiled extension modules are ABI-specific
          # to their build interpreter's version).
          PYTHON_EXE=$(command -v python3)
          echo "Building Python bindings against: $PYTHON_EXE ($($PYTHON_EXE --version))"
          # Fresh build directory: avoids picking up a stale .so left over
          # from an earlier configure against a different Python version.
          rm -rf build
          mkdir -p build
          cd build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=$PWD/install \
            -DDR_EVT_BUILD_PYTHON=ON \
            -DDR_EVT_ENABLE_PROTOBUF=ON \
            -DDR_EVT_ENABLE_GRPC=ON \
            -DPython3_EXECUTABLE=$PYTHON_EXE

      - name: Build
        run: |
          source venv/bin/activate
          cd build
          make -j$(nproc)
          # Build test binaries
          make test_streaming_api-bin test_batch_vs_streaming-bin
          # gRPC client/server + MPI multi-client/server test harness
          # (already covered by `make -j$(nproc)` above since none of
          # these are EXCLUDE_FROM_ALL, but listed explicitly here too
          # for the same clarity reason as the line above). The MPI
          # harness target only exists if find_package(MPI) succeeded at
          # configure time (see CMakeLists.txt) - `|| true` tolerates it
          # being absent rather than failing this otherwise-redundant
          # clarity line with "no rule to make target".
          make dr_evt_server-bin dr_evt_client-bin
          make test_grpc_multi_client_server-bin || true

      - name: Check build artifacts
        run: |
          ls -lh build/simulator
          ./build/simulator --help || true

      - name: Run Comprehensive Tests
        run: |
          echo "========================================"
          echo "Running Comprehensive Tests (34 tests)"
          echo "========================================"
          ./tests/test_all_dr_evt.sh

      - name: Run Queue Implementation Differential Tests
        run: |
          echo "========================================"
          echo "Comparing deque/multimap/block/circular queue implementations"
          echo "========================================"
          ./tests/test_fcfs_comprehensive.sh --correctness

      - name: Run Column Alias Tests
        run: |
          echo "========================================"
          echo "Testing time_limit/actual_run_time column-name aliases (8 tests)"
          echo "========================================"
          ./tests/test_column_aliases.sh

      - name: Run Duration/Run Time Mode Tests
        run: |
          echo "========================================"
          echo "Testing duration_mode/run_time_mode interaction and distribution capping (4 tests)"
          echo "========================================"
          ./tests/test_duration_run_time_modes.sh

      - name: Run Unit Tests
        run: |
          echo "========================================"
          echo "Running Unit Tests (7 tests)"
          echo "========================================"
          ./tests/run_unit_tests.sh

      - name: Run Feature Tests
        run: |
          echo "========================================"
          echo "Running Feature Tests (3 tests)"
          echo "========================================"
          ./tests/run_feature_tests.sh

      - name: Run Replay Tests
        run: |
          echo "========================================"
          echo "Running Replay Tests (3 tests)"
          echo "========================================"
          ./tests/run_replay_tests.sh

      - name: Run Config Tests
        run: |
          echo "========================================"
          echo "Running Config Tests (4 tests)"
          echo "========================================"
          ./tests/run_configs_tests.sh

      - name: Run Python API Tests
        run: |
          source venv/bin/activate
          echo "========================================"
          echo "Running Python API Tests (9 tests)"
          echo "========================================"
          ./tests/run_python_tests.sh

      - name: Run Streaming API Tests
        run: |
          echo "========================================"
          echo "Running Streaming API Tests (4 tests)"
          echo "========================================"
          ./tests/run_streaming_tests.sh

      - name: Run gRPC Client/Server Tests
        run: |
          echo "========================================"
          echo "Running gRPC Client/Server Tests (2 tests)"
          echo "========================================"
          ./tests/run_grpc_tests.sh

      - name: Run Scale Tests (Optional)
        continue-on-error: true
        run: |
          echo "========================================"
          echo "Running Scale Tests (6 tests)"
          echo "========================================"
          ./tests/run_scale_tests.sh

      - name: Test Summary
        if: always()
        run: |
          echo "========================================"
          echo "DR_EVT Test Suite Summary"
          echo "========================================"
          echo "Compiler: ${{ matrix.compiler }}"
          echo ""
          echo "Test Results:"
          echo "  Comprehensive: 34 tests (verified 34/34 in a manual session pass)"
          echo "  Queue Impl Differential: 34 tests x 4 queue impls (deque/multimap/block/circular)"
          echo "  Column Aliases: 8 tests (time_limit/actual_run_time accepted column-name variants)"
          echo "  Duration/Run Time Mode: 4 tests (duration_mode=actual overrides run_time_mode; distribution capping at time_limit)"
          echo "  Unit: 7 tests (5/7 - 2 known-broken, incomplete fixture data)"
          echo "  Feature: 3 tests"
          echo "  Replay: 3 tests (methodology also verified against more)"
          echo "  Config: 4 tests (requires -DDR_EVT_ENABLE_PROTOBUF=ON, now enabled)"
          echo "  Python API: 9 tests (requires -DDR_EVT_BUILD_PYTHON=ON)"
          echo "  Streaming API: 4 tests (requires building test_streaming_api)"
          echo "  gRPC Client/Server: 2 tests (requires -DDR_EVT_ENABLE_GRPC=ON; MPI-based multi-client test skipped if MPI unavailable)"
          echo "  Scale: 6 tests, optional (all working - see tests/run_scale_tests.sh)"
          echo ""
          echo "These counts reflect what this workflow attempts to run, not"
          echo "an independently re-verified total on every commit - see"
          echo "tests/README.md and docs/TESTING_GUIDE.md for known"
          echo "limitations and what 'passing' does and doesn't demonstrate."
          echo "========================================"

      - name: Upload test artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-logs-${{ matrix.compiler }}
          path: |
            /tmp/*.csv
            build/Testing/Temporary/
          retention-days: 7
